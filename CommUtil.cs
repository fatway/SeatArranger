using System;
using System.Resources;

namespace SeatArranger
{
    /// <summary>
    /// 公用方法
    /// </summary>
    public class CommUtil
    {
        public CommUtil()
        {
        }
        
        //public static ResourceManager imgResource = new ResourceManager("SeatArranger.GridStatus", this.GetType().Assembly);
            //statusBase = (Image)rm.GetObject("bas");
    }
}
